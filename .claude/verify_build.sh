#!/bin/bash
# Стара збірка Game3 прибрана з робочого дерева 07.09 (історія — у git, github fortixWallet/GAME-1).
# Справжня гра живе в Game/Bureau: гейт — cd Bureau && .venv/bin/python3 tools/gate.py (скіл /gate).
echo "Game3 — лише робочий простір (CLAUDE.md, .claude, _src). Перевірки: Bureau/tools/gate.py"
exit 0
